{"goal":"p.swap = Pair.new(p.second, p.first)","proof":["Pair.new(p.second, p.first) = p.swap"]}
{"goal":"Pair.new(p.second, p.first).swap = Pair.new(p.first, p.second)","proof":["Pair.new(Pair.new(p.second, p.first).second, Pair.new(p.second, p.first).first) = Pair.new(p.second, p.first).swap","Pair.new(p.second, p.first).second = p.first","Pair.new(p.second, p.first).first = p.second"]}
{"goal":"swap_swap","proof":["Pair.new(p.first, p.second) = p"]}
