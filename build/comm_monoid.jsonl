{"goal":"f(Nat.0)","proof":["a.pow(Nat.0) * b.pow(Nat.0) != (a * b).pow(Nat.0) or f(Nat.0)","Nat.0 != Nat.0 or (a * b).pow(Nat.0) = M.1","Nat.0 != Nat.0 or a.pow(Nat.0) = M.1","Nat.0 != Nat.0 or b.pow(Nat.0) = M.1","M.1 * b.pow(Nat.0) = b.pow(Nat.0)","(a * b).pow(Nat.0) = M.1","a.pow(Nat.0) = M.1","b.pow(Nat.0) = M.1","a.pow(Nat.0) * b.pow(Nat.0) != (a * b).pow(Nat.0)"]}
{"goal":"a * b * (a.pow(k) * b.pow(k)) = a * a.pow(k) * (b * b.pow(k))","proof":["a * a.pow(k) * (b * b.pow(k)) = a * (a.pow(k) * (b * b.pow(k)))","a * b * (a.pow(k) * b.pow(k)) = a * (b * (a.pow(k) * b.pow(k)))","b * b.pow(k) * a.pow(k) = b * (b.pow(k) * a.pow(k))","a.pow(k) * (b * b.pow(k)) * a = a * (a.pow(k) * (b * b.pow(k)))","a.pow(k) * (b * b.pow(k)) = b * b.pow(k) * a.pow(k)","b * (a.pow(k) * b.pow(k)) * a = a * (b * (a.pow(k) * b.pow(k)))","b.pow(k) * a.pow(k) = a.pow(k) * b.pow(k)"]}
{"goal":"(a * b).pow(k.suc) = a.pow(k.suc) * b.pow(k.suc)","proof":["not f(k) or a.pow(k) * b.pow(k) = (a * b).pow(k)","(a * b).pow(k) * (a * b).pow(Nat.1) = (a * b).pow(k + Nat.1)","k.suc != k.suc or a * a.pow(k) = a.pow(k.suc)","k.suc != k.suc or b * b.pow(k) = b.pow(k.suc)","a.pow(k) * b.pow(k) * (a * b) = a * b * (a.pow(k) * b.pow(k))","b * b.pow(k) * (a * a.pow(k)) = a * a.pow(k) * (b * b.pow(k))","b.pow(k.suc) * a.pow(k.suc) = a.pow(k.suc) * b.pow(k.suc)","k + Nat.1 = k.suc","(a * b).pow(Nat.1) = a * b","a * a.pow(k) = a.pow(k.suc)","b * b.pow(k) = b.pow(k.suc)","a.pow(k) * b.pow(k) = (a * b).pow(k)"]}
{"goal":"f(k.suc)","proof":["a.pow(k.suc) * b.pow(k.suc) != (a * b).pow(k.suc) or f(k.suc)","a.pow(k.suc) * b.pow(k.suc) != (a * b).pow(k.suc)"]}
{"goal":"f(c)","proof":["let s0: Nat -> Bool -> Nat satisfy { forall(x0: Nat -> Bool, x1: Nat) { not x0(Nat.0) or x0(s0(x0)) or x0(x1) } and forall(x2: Nat -> Bool, x3: Nat) { not x2(s0(x2).suc) or not x2(Nat.0) or x2(x3) } }","not f(s0(f)) or f(s0(f).suc)","not f(s0(f).suc) or not f(Nat.0) or f(c)","not f(s0(f).suc)","not f(Nat.0) or f(s0(f)) or f(c)","not f(s0(f))","f(s0(f))"]}
{"goal":"pow_distrib_mul","proof":["not f(c) or a.pow(c) * b.pow(c) = (a * b).pow(c)"]}
